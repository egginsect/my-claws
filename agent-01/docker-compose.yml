services:
  agent:
    image: openclaw:local
    environment:
      - OPENCLAW_GATEWAY_TOKEN=${OPENCLAW_GATEWAY_TOKEN}
    volumes:
      - ./.openclaw:/home/node/.openclaw
    ports:
      - "${AGENT_PORT:-18789}:18789"
    restart: unless-stopped

#!/bin/sh
# pre-commit: Encrypt openclaw.json and workspace MD files
# Supports multiple agent directories (e.g., coder, assistant)

set -e

# Ensure PATH includes common locations for Homebrew/1Password
export PATH="$PATH:/opt/homebrew/bin:/usr/local/bin"

# Use secure key location (user's home directory or environment# 1. Try environment variable
if [ -n "$SOPS_AGE_KEY" ]; then
    echo "🔑 Using SOPS key from environment variable"
    export SOPS_AGE_KEY="$SOPS_AGE_KEY"
# 2. Try 1Password
elif command -v op >/dev/null && op whoami >/dev/null 2>&1; then
    echo "🔑 Fetching SOPS key from 1Password..."
    # Attempt to read key, capturing stderr to a temp file to avoid leaking key on stdout but showing error
    if key=$(op read "op://Develop/openclaw-sops-key/password"); then
        export SOPS_AGE_KEY="$key"
    else
        echo "❌ 1Password retrieval failed."
        exit 1
    fi
# 3. Fallback to local key file (legacy/optional)
elif [ -f "$SOPS_AGE_KEY_FILE" ]; then
    echo "🔑 Using SOPS key from file: $SOPS_AGE_KEY_FILE"
    export SOPS_AGE_KEY_FILE="$SOPS_AGE_KEY_FILE"
elif [ -f "key.txt" ]; then
     echo "🔑 Using local key.txt"
     export SOPS_AGE_KEY_FILE="key.txt"
else
    echo "❌ No SOPS key found!"
    echo "   Please sign in to 1Password ('op signin') or set SOPS_AGE_KEY_FILE."
    exit 1
fi

# Find all .openclaw directories to identify agents
find . -type d -name ".openclaw" -not -path "*/node_modules/*" | while read -r openclaw_dir; do
    # 1. Encrypt openclaw.json (only if changed)
    config_file="$openclaw_dir/openclaw.json"
    if [ -f "$config_file" ]; then
        enc_file="${config_file%.json}.enc.json"
        # Only encrypt if source is newer or encrypted file doesn't exist
        if [ ! -f "$enc_file" ] || [ "$config_file" -nt "$enc_file" ]; then
            # Security: Check target is not a symlink before writing
            if [ -L "$enc_file" ]; then
                echo "❌ Error: Refusing to overwrite symlink at $enc_file"
                exit 1
            fi
            echo "🔒 SOPS: Encrypting configuration in $config_file..."
            sops -e "$config_file" > "$enc_file"
            git add "$enc_file"
        fi
    fi

    # 2. Encrypt Workspace MD files (maxdepth 1 only, only if changed)
    workspace_dir="$openclaw_dir/workspace"
    if [ -d "$workspace_dir" ]; then
        find "$workspace_dir" -maxdepth 1 -name "*.md" -not -name "*.enc.md" | while read -r file; do
            enc_file="${file%.md}.enc.md"
            # Only encrypt if source is newer or encrypted file doesn't exist
            if [ ! -f "$enc_file" ] || [ "$file" -nt "$enc_file" ]; then
                # Security: Check target is not a symlink before writing
                if [ -L "$enc_file" ]; then
                    echo "❌ Error: Refusing to overwrite symlink at $enc_file"
                    exit 1
                fi
                echo "🔒 SOPS: Encrypting workspace file $file..."
                sops -e "$file" > "$enc_file"
                git add "$enc_file"
            fi
        done
    fi
done

echo "✅ SOPS: Encryption complete."
